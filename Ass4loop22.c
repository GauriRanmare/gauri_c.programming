#include<stdio.h>
int main()
{
    int n,digit,evendigit=0,odddigit=0,zerodigit=0;

    printf("Enter a Number:");
    scanf("%d",&n);
     while(n>0)
     {
        digit=num%10;

        if(digit)
     }
}